##### Point Types

    SITE  site
    BNET BACnet network
    BHWD BACnet hardware device
    BHWRD BACnet hardware router device
    BAI      BACnet analog input
    BAO      BACnet analog output
    BAV      BACnet analog value
    BBI      BACnet binary input
    BBO      BACnet binary output
    BBV      BACnet binary value
    BMSI     BACnet multistate input
    BMSO     BACnet multistate output
    BMSV     BACnet multistate value
    BCAL     BACnet calendar obj
    BCMD     BACnet command obj
    BDEV     BACnet device obj
    BEE      BACnet Event enrollment obj
    BFIL     BACnet File obj
    BGRP     point group
    BLOP     BACnet Loop object
    BNC      BACnet notification class
    BPRG     BACnet program obj
    BSCH     BACnet schedule obj
    BTRN     BACnet trend log
    BHWR     BACnet Hardware Router
    BHWRDEF  BACnet Hardware Router Definition
    BHWRD    BACnet Hardware Router Device
    BHWRDDEF BACnet Hardware Router Device Definition
    BHWH     BACnet Hardware Half Router
    BHWHDEF  BACnet Hardware Half Router Definition
    BHWHD    BACnet Hardware Half Router Device
    BHWHDDEF BACnet Hardware Half Router Device Definition
    BHWE     BACnet Hardware Expander
    BHWEDEF  BACnet Hardware Expander Definition
    BFM      BACnet Floating Motor
    BPWM     BACnet Pulse Width Modulation
    BTLO     BACnet Time Local Override
    BPTA     BACnet Pulse to Analog
    BLSTAT   BACnet Logistat
    BMAI     BACnet Modeled analog input
    BMAO     BACnet Modeled analog output
    BMAV     BACnet Modeled analog value
    BMBI     BACnet Modeled binary input
    BMBO     BACnet Modeled binary output
    BMBV     BACnet Modeled binary value
    BMTRN    BACnet Modeled trend
    BDRV     BACnet module driver
    BAIRFLOW BACnet Airflow
    BMCAL    BACnet Modeled calendar
    BMCMD    BACnet Modeled command
    BMDEV    BACnet Modeled device
    BMEE     BACnet Modeled event enrollment
    BMFIL    BACnet Modeled file
    BMGRP    BACnet Modeled group
    BMLOP    BACnet Modeled loop
    BMNC     BACnet Modeled notification class
    BMPRG    BACnet Modeled program
    BMSCH    BACnet Modeled schedule
    BMAVG    BACnet Modeled average
    EVT      Event Microblock (BACnet Binary Value)
    BRS      BACnet Zone Senese (AKA Logistat II)
    BAF      BACnet Air Flow - has BAI and BTB/BTB_PD/BTB_BC under it
    BTB      ALC proprietary Test and Balance object - used in a BAF
    BRSF     BACnet Zone Senese "RS-Pro-F" - like BRS but has added fan control and more!
    BSPT     BACnet SetpointMicroblock - new microblock introduced for 3Vopen
    BPD      BACnet Pressure Dependent microblock
    BTB_PD   ALC proprietary Test and Balance object (pressure dependent)
    BTB_BC   ALC proprietary Test and Balance object (bypass controller)
    BBYPASS  BACnet Bypass airflow microblock
    BRSH     BACnet Zone Senese (AKA Logistat II) w/ humidity
    BRSFH    BACnet Zone Senese "RS-Pro-F" - like BRSH but has added fan control and more!
    COLLECTOR    BACnet Collector
    PROVIDER     BACnet Provider
    BPID    BACnet PID microblock   
    BMACC    BACnet Modeled accumulator
    BMPC     BACnet Modeled pulse converter
    BMDA     Device Alias microblock
    BSVI     BACnet binary sensed value input (Combo Sensors mb)
    ASVI     BACnet analog sensed value input (Combo Sensors mb)
    SNB      Sensor Binder (Combo Sensors mb)
    BSPTCS   BACnet Set Point mb for combo sensors
    BMSTV    BACnet Structured View Modeled object
    BMLC     BACnet Load Control Modeled Object
    BMEL     BACnet Event Log Modeled Object
    BMTLM    BACnet Trend Log Multiple Modeled Object
    PGRP    Property group - simple non object microblock structure